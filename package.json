{
  "name": "input-sanitizer",
  "version": "1.0.0",
  "description": "Input Sanitizer",
  "type": "module",
  "scripts": {
    "sanitize": "bun run src/cli.ts",
    "generate-dataset": "bun run src/generate-dataset.ts",
    "evaluate": "bun run src/evaluate.ts",
    "evaluate:llama-guard": "bun run src/evaluate.ts --llama-guard",
    "demo": "bun run src/demo.ts"
  },
  "dependencies": {
    "ai": "^4.0.0",
    "@ai-sdk/openai-compatible": "^0.2.0",
    "@ai-sdk/groq": "^1.0.0",
    "ollama-ai-provider-v2": "^1.5.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}